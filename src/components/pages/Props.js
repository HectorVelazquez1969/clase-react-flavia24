const EjemploProps1 = props => {
    return(
        <h3>Hola {props.nombre}</h3>
    )
}

export{
    EjemploProps1´
    //aca usamos {} y no export porque son varios componentes distintos losque vamos a exportar
}